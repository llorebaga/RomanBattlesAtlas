# Dependencies explicitly approved to run install/build scripts. pnpm blocks
# these by default as a supply-chain safeguard; listing them here acknowledges
# them so `pnpm install` does not fail with ERR_PNPM_IGNORED_BUILDS.
onlyBuiltDependencies:
  - esbuild
  - sharp
  - unrs-resolver
  - workerd
